# Extraction des URLs API depuis les APK Flutter (libapp.so)

```bash
cd docker/android-emulator
node scripts/extract-api-from-apk.mjs apk/extracted-sky-arm64/lib/arm64-v8a/libapp.so
```

Voir aussi `scripts/probe-realmnovel-api.mjs` pour sonder realmnovel.com.
